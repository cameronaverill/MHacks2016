import nltk
from nltk import sent_tokenize, word_tokenize, pos_tag

sample_text = "Except at the pores, the nuclear side of the envelope is lined by the nuclear lamina, a netlike array of protein laments that maintains the shape of the nucleus by mechanically supporting the nuclear envelope"

def __init__():
	sents = sent_tokenize(sample_text)
	terms = 
	defs = [sent for sent in sents if term in sent]
	sample_text_text = nltk.Text(tokens)
	print sample_text_text.concordance('lamina', 500)
	
	

if __name__ == "__main__":
	__init__()
